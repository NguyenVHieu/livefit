const API = 'https://app.livefit.vn/api/'
//* *********************** */

const REGISTER = `${API}users/register`
const LOGIN = `${API}users/login`
const GET_USER = `${API}users/profile`
const FORGOT_PASSWORD = `${API}users/forgot-password`
const UPDATE_PROFILE = `${API}users/update-profile`
export {
  REGISTER,
  LOGIN,
  GET_USER,
  FORGOT_PASSWORD,
  UPDATE_PROFILE
}
